---
page_title: "tigris_iam_policy Resource - Tigris"
subcategory: ""
description: |-
  Provides a Tigris IAM policy resource.
---

# tigris_iam_policy (Resource)

Provides a Tigris IAM policy resource.

## Example Usage

```terraform
# Create an IAM policy that allows read/write access to a bucket.
resource "tigris_iam_policy" "upload_only" {
  name        = "upload-only"
  description = "Allow upload and read operations for the landing zone bucket"

  statement {
    effect = "Allow"
    actions = [
      "s3:PutObject",
      "s3:GetObject",
      "s3:ListBucket",
    ]
    resources = [
      "arn:aws:s3:::landing-zone.example.com",
      "arn:aws:s3:::landing-zone.example.com/*",
    ]
  }
}
```
<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) The IAM policy name.
- `statement` (Block List, Min: 1) The policy statements for this document. (see [below for nested schema](#nestedblock--statement))

### Optional

- `description` (String) The IAM policy description.
- `timeouts` (Block, Optional) (see [below for nested schema](#nestedblock--timeouts))

### Read-Only

- `arn` (String) The ARN of the IAM policy.
- `id` (String) The ID of this resource.
- `policy_id` (String) The IAM policy identifier.

<a id="nestedblock--statement"></a>
### Nested Schema for `statement`

Required:

- `actions` (List of String) The S3 actions granted or denied by this statement.
- `effect` (String) The statement effect. Must be Allow or Deny.
- `resources` (List of String) The resource ARNs referenced by this statement.


<a id="nestedblock--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `create` (String)
- `delete` (String)
- `read` (String)
- `update` (String)

## Import

Import is supported using the following syntax:

```shell
terraform import tigris_iam_policy.upload_only arn:aws:iam::123456789012:policy/upload-only
```
